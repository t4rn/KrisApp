@model KrisApp.Models.Articles.ArticleHomeModel

@{
    ViewBag.Title = "Zaawansowane programowanie";
}



<div class="jumbotron">
    <h1>@Model.MainArticle.Title</h1>
    <p class="lead">@Model.MainArticle.Description</p>
</div>


@*@for (int i = 0; i < Model.Count() - 1; i++)
    {
        if (i == 0 || i % 3 == 0)
        {
            @Html.Raw("<div class=row>");
        }
        <div class="col-md-4">
            <h2>@Model.ElementAt(i).Name</h2>
            <p>@Model.ElementAt(i).Description</p>
            <p>@Html.ActionLink("Przejdź", Model.ElementAt(i).Code, "Article", null, new { @class = "btn btn-success" })</p>
        </div>

        if (i % 3 == 0)
        {
            @Html.Raw("</div>");
        }
    }*@

@foreach (var item in Model.SideArticles)
{
    <div class="row">

            @foreach (var a in item)
                {
                <div class="col-md-4">
                    <h2>@a.Title</h2>
                    <p>@a.Description</p>
                    <p>@Html.ActionLink("Przejdź", a.Code, "Article", null, new { @class = "btn btn-success" })</p>
                </div>
            }

    </div>
}
